arch:
  pacman:
    - htop
    - curl
    - wget
    - git
    - vim
    - tmux
    - openssh
  aur:
    # No AUR packages for this server
  custom:
    - ensure_sshd_running

windows:
  winget:
    # No Windows packages for this Linux server