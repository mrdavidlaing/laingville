arch:
  pacman:
    - htop
    - curl
    - wget
    - git
    - vim
    - tmux
    - openssh
  aur:
    # No AUR packages for this server
  custom:
    - ensure_sshd_running
    - ensure_hosts_configured

windows:
  winget:
    # No Windows packages for this Linux server