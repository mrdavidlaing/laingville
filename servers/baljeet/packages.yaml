arch:
  pacman:
    - htop
    - curl
    - wget
    - git
    - vim
    - tmux
    - openssh
  aur:
    - k3s-bin
    - k9s
  custom:
    - ensure_k3s_running
    - ensure_sshd_running
    - ensure_hosts_configured

windows:
  winget:
    # No Windows packages for this Linux server