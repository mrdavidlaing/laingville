arch:
  pacman:
    - htop
    - curl
    - wget
    - git
    - vim
    - tmux
    - openssh
    - geoip
  yay:
    # No AUR packages currently needed
  custom:
    - ensure_sshd_running

windows:
  winget:
    # No Windows packages for this Linux server