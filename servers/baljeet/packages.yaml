arch:
  pacman:
    - htop
    - curl
    - wget
    - git
    - vim
    - tmux
    - openssh
    - bind
  yay:
    # No AUR packages currently needed
  custom:
    - ensure_sshd_running
    - ensure_bind_configured

windows:
  winget:
    # No Windows packages for this Linux server