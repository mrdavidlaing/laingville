arch:
  pacman:
    - htop
    - curl
    - wget
    - git
    - vim
    - tmux
    - openssh
  aur:
    # No AUR packages for this server
  custom:
    # No custom packages for this server

windows:
  winget:
    # No Windows packages for this Linux server