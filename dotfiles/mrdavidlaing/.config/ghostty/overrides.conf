# Personal Ghostty overrides (loaded last; wins over base config/theme).

# Larger font for macOS (equivalent to pressing Cmd+ eight times from 13).
font-size = 18

# Clickable URLs (Cmd+click to open)
link-url = true

# Fancy window chrome - minimal titlebar
macos-titlebar-style = hidden

