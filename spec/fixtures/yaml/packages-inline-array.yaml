# Package configuration with inline array format
# Tests that parsers handle [item1, item2] syntax
# Note: Bash parser currently does NOT support this format

windows:
  winget: [Git.Git, Microsoft.PowerShell]
  scoop: [git, versions/wezterm-nightly]
  psmodule: [PowerShellGet, Pester]

arch:
  pacman: [git, vim, tmux]
  yay: [starship, yay]