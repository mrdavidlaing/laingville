{
  "$schema": "https://opencode.ai/config.json",
  "formatter": {
    "shell-scripts": {
      "command": ["./scripts/format-files.sh", "$FILE"],
      "extensions": [".sh", ".bash", ".ps1"]
    }
  }
}
